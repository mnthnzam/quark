# QUARK — log of 21 Sep 2026 (frozen)

Record of the day the project was opened, written before the repo had an operating layer. **Closed — do not append.** Written while the work sat in the CISCO project by mistake; every CISCO path below is dead. The call transcript it cites is committed as a Quark-only excerpt (`sources/2026-09-21_Crossover-Weekly_Quark-excerpt.txt`). From 22 Sep the trail is `STATE.md` + `docs/LEDGER.md` + `git log`. IDs like DEC-16 refer to the pre-retrofit decision table; the surviving decisions are in `DECISIONS.md`.

---

## 2026-09-21 (Mon) — project opened

**In**
- Transcript, Zamstars / Crossover weekly, 21 Sep → `sources/2026-09-21_Crossover-Weekly_Transcript-21.txt`. Quark section runs 16:41–56:30; the rest of the call is Crossover work and is **not** tracked here.
- quark.com scanned by text fetch: `/`, `/products/quarkxpress`, `/whats-new-quarkxpress-latest-version`, `/shop`, `/renewals`.

**Done**
- Side project scaffolded at `quark/` inside the CISCO folder (moved to its own repo the same day).
- `CONTEXT.md`, `WORKLIST.md` (42 items, 6 workstreams), `DECISIONS.md` (16 decisions, 14 open questions) written from the transcript.
- `audit/site-audit_seed.csv` seeded from the scan, traffic-lit per Andrew's scheme.

**Found by the scan, not raised on the call**
- Infografix is linked in four places, not two: header, footer, a homepage block, and the product page add-ons section.
- Shop buy-buttons still carry `x-campaign=26AprilPromo`.
- Language switcher lists ~40 languages. If hand-maintained, this is the largest hidden scope item.
- What's-new page is a running changelog (v20 → v22.0.1), so the update is an added block, not a rewrite.
- Reviews on the product page are four Gartner iframes — likely the "broken reviews" Andrew mentioned.
- No upgrade SKU or upgrade form visible on shop or renewals. Both hand off to Cleverbridge.
- Homepage awards strip is dated 2023–2024.

**Caveats**
- Scan is text-only through a summarising fetcher. No visual confirmation, no JS-rendered content (prices, currency control). Every `scan` row wants a human look before it's acted on.
- Transcript is machine-generated. "Updating 25 to 26" / "2026 to 2027" were examples, not the release name.

**Flag on location (DEC-16)**
- This is a different client sitting in the Cisco project. Risks: Cisco sessions load Quark context and vice versa; shared git history; project knowledge search returns cross-client hits. Mitigated by the isolation note in `README.md` and one-line pointers only in CISCO files. If Quark runs past October, move it to its own project.

**Next**
- Manthan: A-01 email shell, A-02 product page shell, A-05 video recommendation — none need Quark's messaging.
- Extend the audit beyond five pages (trial, academic/charity/government, tutorials, AI-in-QuarkXPress, AI FAQs, capability pages, use-case pages, installers, documentation).
- When messaging lands: drop it in `sources/`, unblock everything tagged MSG.
- When Andrew's Pardot scoping lands: drop in `sources/`, unblock PARDOT items.

---

## 2026-09-21 (Mon) — session 2: audit pass 2 + Figma shells v1

**In**
- Figma file from Manthan: `https://www.figma.com/design/w7KtCUEKUuXt2mlkhzg8SL/Quark-Website-x-Product` — homepage + QuarkXPress product page. It is an HTML capture of the live pages (absolute layout, no components, styles or variables). Prices captured in ₹.

**Done — audit**
- Mapped 649 quark.com URLs (`audit/urls_raw.txt`). Read ~45 QuarkXPress-related pages: link extraction + main-content text, live fetch.
- `audit/site-audit_seed.csv` → `audit/site-audit.csv`, 43 → 68 rows (19 green · 19 yellow · 14 orange · 7 red · 9 TBD).
- **Security finding**: `/academic` has 9 hidden outbound links to unrelated third-party sites. Confirmed by raw link list; not on 39 other pages checked. Logged B-16.
- Q-03 answered (WPML, 7 languages). Q-05 partly answered (geo-localised price on one URL). B-06 confirmed (Gartner embeds return a DNS error).
- New scope nobody raised: press release + launch blog (B-19), installers (B-17), documentation (B-18). Five use-case pages are evergreen — off the list.

**Done — design (Figma, new section "Release shells — 21 Sep", node 3:2)**
- A · Product page release version (node 3:3): hero + features cloned; eyebrow row, year badge, quote slot, rows 1–3 as new-feature slots with screenshot chips, play buttons + Watch Video hidden, CTA year.
- B · Homepage release slide (3:67): slide 1 of 2; current hero becomes slide 2.
- C · Announcement email master shell, 600px (5:2): 7 blocks; 04 and 06 vary by audience.
- Originals (sections 1:1599, 1:2096) untouched. Notes in `design/DESIGN-NOTES.md`.

**Caveats**
- All copy in the shells is bracketed placeholder. "20XX" = unknown release name.
- Email shell is not yet checked against Quark's existing Pardot templates — Andrew pulls those 22 Sep. Expect the block order to bend to whatever their template already supports.
- First clone attempt distorted (children had scale constraints); deleted and redone. Only my own nodes were removed.
- Audit still has not had a human eyeball pass. The `/ai-in-quarkxpress` "v21.1" row comes from a summarised fetch — check before editing.

**Next**
- Vinodh: B-16 today.
- When MSG lands: fill shells A/B/C, write F-01 once, reuse across product page rows and email rows.
- When Pardot HTML lands: reconcile shell C with the real template; then build HTML.

